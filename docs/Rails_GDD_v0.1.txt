# Abyss Rail: Mini GDD 🚀🐙

**Version:** 0.1 (Prototype)  
**Date:** Nov 11, 2025  
*Edit away!*

## High Concept
**Starfox meets the deep blue**—pilot an ROV on rails through a pitch-black abyss. Dodge jagged cliffs, blast creepy aliens. Your lights pierce the fog, forks split the path. A tense, heart-pounding descent shooter.

## Core Loop
1. **Auto-dive** along a `Path3D` (tweened progress).
2. Mouse/gamepad sets a **local target** → ship *lerps* in for smooth chase.
3. **Shoot** forward (-Z), **dodge roll** (360° on Z), **boost/brake** (speed + cam zoom).
4. Smash into triggers/enemies → explosions & events.
5. **Fork choice**: left/right → reparent to new path.

**Win:** Survive the level.  
**Lose:** Health hits zero.

## Mechanics

### Movement
- **TargetMarker**: Instant input, clamped in local space.
- **Ship lerp**: Variable speed (boost ×2, brake ×0.5).
- **Rotate**: `atan2` from X/Y diff.

### Shooting
- Raycast or spawn projectiles from `GunSpawn` (`ship.basis * -Z`).

### Dodge
- Tween `rot.z` 0→360° (5s cooldown). Invuln frames? 🛡️

### Boost / Brake
- Input lerps speed + camera distance (far = boost, close = brake).

### Triggers
- `Area3D` pawn hits static `Area`s on path → dialogue, cave-in, explosions.

### Enemies
- **SpawnerManager**: Waves based on `progress_ratio`.
- **Pre-stage**: Activate hidden enemies at `path.sample_baked(ahead)`.

### Controls
- **Aim**: Mouse / gamepad position  
- **Shoot**: LMB  
- **Dodge**: A/D (or similar)  
- **Boost**: RT  
- **Brake**: LT

## Tech Stack (Godot 4.x)

| Node / Plugin | Purpose |
|---------------|---------|
| `Path3D` / `Follow3D` | Rails + pawn |
| `TargetMarker` | Input target |
| `Ship` | Mesh / Light / Gun / Visuals / Shoot |
| `Camera3D` | Dolly chase |
| `Terrain3D` | Floor / cliffs |
| `EnemySpawner` | Waves |
| **Plugins** | Terrain3D (AssetLib), Phantom Cam? (opt), DebugDraw3D |

## Prototype Goals
- [ ] Flyable ship: lerp, rot, boost, dodge  
- [ ] Path + fork choice  
- [ ] 1 trigger (boom!)  
- [ ] 3 enemies: shootable + 1 turret  
- [ ] Health / UI basics  

**Stretch:** Caustics, fog, particles 🌊✨

## Art / Sound Vibes
- **Dark fog**, piercing **SpotLight** beams  
- Silt floor + cliff rocks (Terrain paint)  
- Bubbles & explosions via **GPUParticles** 💥